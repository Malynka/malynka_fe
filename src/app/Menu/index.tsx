import React, {FunctionComponent, PropsWithChildren} from 'react';
import { MenuContainer } from './styles';

export interface IMenuProps {

}

const Menu: FunctionComponent<PropsWithChildren<IMenuProps>> = () => {

  return (
    <MenuContainer>
      
    </MenuContainer>
  );
};

export default Menu;