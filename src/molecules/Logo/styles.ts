import styled from "styled-components";

export const LogoContainer = styled.div`
  display: flex;
  column-gap: 16px;
  justify-content: center;
  align-items: center;
  padding: 24px 20px;

  user-select: none;
  pointer-events: none;
`;