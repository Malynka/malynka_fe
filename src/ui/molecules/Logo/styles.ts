import styled from "styled-components";

export const LogoContainer = styled.div`
  display: flex;
  column-gap: 16px;
  justify-content: center;
  align-items: center;
  padding: 16px;

  user-select: none;
  pointer-events: none;
`;